name: format and lint

on:
  push:
    branches: [ work ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Install dependencies
        run: npm ci

      - name: ESLint
        run: npm run lint